import SettingsPageSkeleton from "./_components/SettingsPageSkeleton";

export default function Loading() {
  // This loading component will be automatically rendered by Next.js
  // during navigation to the settings page while data is being fetched.
  return <SettingsPageSkeleton />;
}
